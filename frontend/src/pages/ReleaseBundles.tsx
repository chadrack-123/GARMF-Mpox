import { Container, Typography } from '@mui/material'

export default function ReleaseBundles() {
  return (
    <Container maxWidth="xl">
      <Typography variant="h4" gutterBottom>
        Release Bundles
      </Typography>
      <Typography variant="body1">
        Download complete release bundles with all artefacts, documentation, and reproduction instructions.
      </Typography>
    </Container>
  )
}
